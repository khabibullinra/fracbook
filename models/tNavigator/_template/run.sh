#!/usr/bin/env bash
# TODO: Реализовать запуск модели tNavigator.
#
# Типичные шаги для модели tNavigator:
#   1. Подготовка окружения (переменные, PATH до бинаря tNavigator).
#   2. (Опционально) Конвертация / генерация grid/include файлов.
#   3. Запуск симулятора на data/MODEL.data.
#   4. Извлечение результатов (SUMMARY, RSM, графики).
#
# Бинарные артефакты (UNRST, EGRID, RSM, логи) — НЕ коммитить,
# они уже в корневом .gitignore.

set -euo pipefail

echo "TODO: реализовать запуск модели tNavigator." >&2
echo "См. models/tNavigator/_template/README.md для конвенций." >&2
exit 1
