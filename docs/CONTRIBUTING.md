# Роли участников

В репо нет Git-уровневых ограничений — все доверенные. Роли — логические, для координации.

## Преподаватель-редактор (один)

- Отвечает за содержание `book/`.
- Принимает PR в `book/`.
- Пушит в `pages` через `tools/publish-pages.sh`.
- Откатывает случайные пуши в `pages` (если студент ошибся веткой).

## Преподаватели-разработчики (несколько)

- Развивают `src/fracbook/`.
- Ведут `models/`.
- Принимают PR в `src/` и `models/`.
- Участвуют в обсуждении архитектуры через ADR в `docs/decisions/`.

## Студенты

- Помогают с материалами: `book/chapters/`, ноутбуки в `notebooks/drafts/`.
- Открывают PR, ждут ревью.
- **Не пушат в `pages`** (это роль редактора).

## Все участники

- Перед PR прогоняют локально:
  ```bash
  pytest -q
  ruff check src tests
  quarto render book/
  ```
- Используют `.venv` в корне проекта. Никаких системных установок `fracbook`.
- Перед коммитом убеждаются, что в `master` не попадут:
  - `.venv/`, `_book/`, `_freeze/`, `_site/`, `.ipynb_checkpoints/`;
  - бинарь симуляторов (`.geos`, `.unrst`, `.EGRID`, `.INIT`, логи);
  - крупные (>10 МБ) выходные данные ноутбуков.

Подробные соглашения по стилю кода и книги — в `book/CONTRIBUTING.md`.
